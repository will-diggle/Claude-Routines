name: Build & Submit to TestFlight

on:
  workflow_dispatch:
  push:
    branches:
      - claude/build-app-from-brief-wNEna
    paths:
      - 'bilinguist-brief/src/**'
      - 'bilinguist-brief/assets/**'
      - 'bilinguist-brief/App.tsx'
      - 'bilinguist-brief/app.json'
      - 'bilinguist-brief/package.json'
      - 'bilinguist-brief/babel.config.js'

jobs:
  build-and-submit:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: bilinguist-brief
        run: rm -f package-lock.json && npm install

      - name: Inject API keys
        working-directory: bilinguist-brief
        env:
          DEEPL_API_KEY: ${{ secrets.DEEPL_API_KEY }}
          ELEVENLABS_API_KEY: ${{ secrets.ELEVENLABS_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          [ -n "$DEEPL_API_KEY" ] && echo "EXPO_PUBLIC_DEEPL_API_KEY=$DEEPL_API_KEY" >> .env.local || true
          [ -n "$ELEVENLABS_API_KEY" ] && echo "EXPO_PUBLIC_ELEVENLABS_API_KEY=$ELEVENLABS_API_KEY" >> .env.local || true
          [ -n "$ANTHROPIC_API_KEY" ] && echo "EXPO_PUBLIC_ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY" >> .env.local || true

      - name: Build iOS app
        working-directory: bilinguist-brief
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: npx eas-cli build --platform ios --profile production --non-interactive

      - name: Submit to TestFlight
        working-directory: bilinguist-brief
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: npx eas-cli submit --platform ios --latest --non-interactive
