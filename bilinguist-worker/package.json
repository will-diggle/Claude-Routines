{
  "name": "bilinguist-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "populate": "npx tsx scripts/populate.ts"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@cloudflare/workers-types": "^4.20241127.0",
    "tsx": "^4.0.0",
    "wrangler": "^4.0.0"
  }
}
